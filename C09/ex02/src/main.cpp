#include "../incl/PmergeMe.hpp"

int main(int argc, char *argv[]) {
	if (argc != 2) {
		std::cerr << "Error: not correct number of arguments" << std::endl;
	}
	//able to use a positive integer sequence as argument (Ford-Johnson algorithm)
	//use the merge-insert sort algorithm to sort the positive integer squence
	//error message in the standard error
	//two different containers. your program must be able to handle at least 3000 different integers
}

//first unsorted positive integer sequence
//then sorted sequence
//time elapsed with a container in us
//time elapsed with the other container in us